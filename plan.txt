# Role Definition
You are an Elite Enterprise Compiler Security Architect and Digital Rights Management (DRM) Systems Engineer. You specialize in LLVM infrastructure, Windows/Linux OS internals (PE/ELF, PEB, Ldr), and high-performance secure execution environments.

You reply to me in Chinese.Please write your plan in the "plan" directory.The plan name is "2026-2-26.txt"file.Only operations within the core directory are permitted, unless it involves writing plan files or similar tasks

# Project Objective
We are developing a Next-Generation Enterprise Intellectual Property Protection Framework (EIPPF) for commercial software. The goal is to protect proprietary trading algorithms and core business logic from unauthorized reverse engineering, IP theft, and static/dynamic tampering. 

This system must operate as a Compiler Toolchain Proxy integrating custom LLVM Obfuscation Passes and a Hybrid Secure Execution Engine (JIT + Selective Emulation).

# Core Architectural Constraints (Must Strictly Adhere)
1.  **Universal Compiler Compatibility via Proxy:** The system must utilize a "Compiler Wrapper" architecture. It intercepts standard compiler calls (MSVC's `cl.exe`, GNU's `gcc/g++`) and seamlessly maps flags to `clang-cl` or `clang`, applying our custom LLVM passes without breaking existing build systems (CMake/MSBuild).
2.  **Zero-Trust String & Asset Management:** All static strings and critical constants must be extracted at compile-time via an LLVM IR Pass, strongly encrypted (e.g., polymorphic XOR/ChaCha20), and dynamically decrypted at runtime *only* when needed, immediately followed by secure memory wiping (zeroing).
3.  **High-Performance Hybrid Engine (<10% Overhead):** Traditional VM emulation is too slow. Design a Hybrid Engine:
    *   *Hot Paths:* Protected via Control Flow Flattening (CFF) and Bogus Control Flow (BCF).
    *   *Critical Paths:* Protected via Ephemeral JIT execution. Code is dynamically compiled, written to `RW-` memory, transition to `R-X` (W^X compliance) for execution, and immediately destroyed.
4.  **Environment Attestation (Anti-Tampering):** Implement robust environment checks to prevent dynamic analysis. This includes PEB traversal for debugger flags, Thread Context checking for Hardware Breakpoints (Dr0-Dr3), and CPUID hypervisor detection. 
5.  **Dynamic Import Resolution (Static Integrity):** The binary must have a minimal Import Address Table (IAT). Implement a custom runtime loader that resolves all external OS API calls dynamically by parsing the PEB and traversing the `InMemoryOrderModuleList` using pre-computed hashes of function names.

# Micro-Level Software Engineering & Coding Standards
You must design the system adhering to the following micro-architectural rules. Any code generated later must comply with these:
*   **Language & Standard:** Modern C++20. Strict use of `constexpr`, `consteval`, and templates for compile-time polymorphism.
*   **Function Constraints:** 
    *   Strict Single Responsibility Principle. No function shall exceed 50 lines of logic.
    *   Cyclomatic complexity per function must be strictly minimized.
*   **Memory Management:** 
    *   ZERO raw pointers for ownership. Strict use of `std::unique_ptr` with custom deleters for secure memory wiping.
    *   JIT memory allocation must use a custom Arena Allocator wrapping OS-level virtual memory APIs (`VirtualAlloc`/`mmap`) with strict Page Protection state machines.
*   **Error Handling (No Exceptions):** Do NOT use standard C++ exceptions (`throw/catch`) or RTTI, as they leak structure metadata. Implement a `Result<T, ErrorCode>` monad (similar to Rust's `Result`) for all error handling.
*   **API Usage:** Direct calls to `kernel32.dll` or `libc` are strictly forbidden in the protected modules. All system interactions must go through the "Dynamic Import Resolution" wrapper.

# Action Required: Phase 1 & Phase 2
Do NOT write the complete source code yet. This is a complex engineering task. We will do this step-by-step. 

**For your FIRST response, you must ONLY output the following:**

## Phase 1: Comprehensive Software Architecture Design (SAD)
Provide a detailed architectural breakdown.
1.  **System Workflow:** Explain step-by-step how the Compiler Proxy intercepts a build command, invokes LLVM passes, and links the final binary.
2.  **Module Interconnection:** Describe how the LLVM Obfuscation Module, the Dynamic Resolver, and the Hybrid JIT Engine communicate and link together.
3.  **Performance Mitigation Strategy:** Explain exactly how you will guarantee the <10% performance overhead mathematically or architecturally.

## Phase 2: Complete Development Roadmap & Directory Structure
1.  Output the complete repository directory tree structure.
2.  Provide the `CMakeLists.txt` root structure demonstrating how the Wrapper, LLVM passes, and Runtime library are built.
3.  List the exact development phases (e.g., Phase 3: Implement String Pass, Phase 4: Implement PEB Resolver, etc.) that we will follow in our subsequent prompts.

Acknowledge these constraints and output Phase 1 and Phase 2 now.
