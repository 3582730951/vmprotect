# 2026-04-05 sample-suite CI Delta-2.5k 批准与分配记录

## 状态
- `APPROVED`

## 触发事实
- `sample-suite-eval` run `23996001377` 已结束为 `failure`
- `build-windows` job `69984057405` 已经不再报 `non-compilable target type`
- 当前单一 blocker 已收敛为：
  - `ninja: error: 'C:/Program Files (x86)/Microsoft Visual Studio/2019/Professional/DIA SDK/lib/amd64/diaguids.lib', needed by '.../eippf_protection_suite_pass.dll', missing and no known rule to make it`

## 批准范围
- 仅批准：
  - `/workspace/core/cmake/PassPluginCompat.cmake`
  - `/workspace/core/passes/CMakeLists.txt`
- 明确禁止：
  - 修改 `/workspace/core/CMakeLists.txt`
  - 修改任何 `.cpp/.hpp/.py/.ps1/.yml`
  - 修改 workflow / validator / build scripts / tests
  - 通过环境修补、增加 `LIB/LIBPATH`、补 `diaguids.lib` 来“过关”

## 三角色最终审核结论
- `Architecture Reviewer`: `PASS`
  - 原因：
    - 作用域只收敛在 Windows pass plugin 链接面
    - `WINDOWS_LLVM_COMPONENTS` allowlist 与 DIA denylist 都已冻结
    - 继续保持真实 `eippf_protection_suite_pass.dll` 与 validator 不变
- `Engineer Reviewer`: `PASS`
  - 原因：
    - 两个文件的改法已经机械化
    - 固定组件清单、静态 gate、local build gate 都已明确
    - engineer 不需要再自行猜组件或扩 scope
- `Security Reviewer`: `PASS`
  - 原因：
    - fail-closed 明确
    - 明确禁止禁用 plugin、改 workflow、补环境
    - allowlist + denylist 不引入新的静默回退

## 回收规则
- 本记录落盘后，三位 reviewer 立即回收，不参与实现。

## 实现分配
- 新工程 owner：`Agent 6 (Engineer)` 新实例
- 文件所有权：
  - `/workspace/core/cmake/PassPluginCompat.cmake`
  - `/workspace/core/passes/CMakeLists.txt`
- 严格禁止：
  - 修改其它文件
  - 修改 workflow / sample-suite / wrapper / top-level CMake
  - 修改 pass 源文件

## PM 验收顺序
1. `git diff --name-only`
2. `git diff --check`
3. `2.5k` 静态 gate
4. `2.5k` local configure gate
5. `2.5k` local `eippf_protection_suite_pass` build gate
6. 三角色实现复审
7. 提交推送
8. 观察新的 `sample-suite-eval`
