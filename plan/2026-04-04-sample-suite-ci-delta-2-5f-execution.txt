# 2026-04-04 sample-suite CI Delta-2.5f 执行记录

## Scope
- delta: `2.5f`
- blocker: `macOS Validate macOS iOS Toolchain Report -> [FAIL] plugin_build_command missing host compiler path`
- implementation file:
  - `/workspace/.github/workflows/sample-suite-eval.yml`

## Plan Approval
- `Agent 1 (Planner / PM)`: `PASS`
- `Agent 2 (Engineer plan reviewer)`: `PASS`
- `Agent 3 (Security Reviewer)`: `PASS`

## Assignment
- `Agent 2 (Engineer)` 获得唯一实现文件所有权：
  - `/workspace/.github/workflows/sample-suite-eval.yml`
- 修改范围冻结为：
  - macOS `Validate macOS iOS Toolchain Report` 的内嵌 Python 段

## Implementation Summary
- 新增：
  - `extract_cmake_define_paths(text: str, define_key: str) -> list[Path]`
  - `assert_cmake_define_matches_expected(text: str, define_key: str, expected_path: Path, fail_msg: str) -> None`
- 固定：
  - `extract_cmake_define_paths` 使用 `shlex.split(text, posix=True)` 与 `^-D<KEY>(?::[^=]+)?=(.+)$`
  - `assert_cmake_define_matches_expected` 只允许 `OSError` / `FileNotFoundError` 触发 fallback
- 替换：
  - 删除 `plugin_build_command` 上两条 `resolve() ... not in plugin_build_text` 字符串包含判断
  - 改为：
    - `CMAKE_CXX_COMPILER` 与 `Path(kv["host_compiler_path"])` 的路径同一性校验
    - `LLVM_DIR` 与 `Path(kv["llvm_dir"])` 的路径同一性校验
- 保持不变：
  - `set(kv) == set(required)` gate
  - `plugin_build_log` strict gate
  - `plugin_link_command` strict gate
  - Linux / Windows / Android validator
  - `build_macos_ios.sh`

## PM Static Validation
- `git diff --name-only`
  - 结果：仅 `.github/workflows/sample-suite-eval.yml`
- `git diff -U0 -- .github/workflows/sample-suite-eval.yml`
  - 结果：仅 macOS validator 段有 diff
- `git diff --check`
  - 结果：通过
- `rg -n 'extract_cmake_define_paths|assert_cmake_define_matches_expected|plugin_build_command missing host compiler path|sidecar missing required token: plugin_build_command|plugin_link_command leaked dead_strip|plugin_link_command leaked ninja progress prefix' /workspace/.github/workflows/sample-suite-eval.yml`
  - 结果：命中新增 helper 与既有 strict gate
- `rg -n 'str\\(Path\\(kv\\[\"host_compiler_path\"\\]\\)\\.resolve\\(\\)\\) not in plugin_build_text|str\\(\\(llvm_prefix / \"lib\" / \"cmake\" / \"llvm\"\\)\\.resolve\\(\\)\\) not in plugin_build_text' /workspace/.github/workflows/sample-suite-eval.yml`
  - 结果：0 命中
- 任务卡固定 `compile(..., "exec")` 语法门禁
  - 结果：`OK`
- `rg -n '^\\s*#$' /workspace/.github/workflows/sample-suite-eval.yml`
  - 结果：0 命中

## Implementation Review
- `Agent 1 (Planner / PM)`: `PASS`
  - 原因：逻辑与任务卡一致；diff 边界正确；固定语法门禁通过
- `Agent 2 (Engineer reviewer)`: `PASS`
  - 原因：新增 helper 与两处判断替换满足任务卡；空行删除虽有格式噪音，但不构成必须打回项
- `Agent 3 (Security Reviewer)`: `PASS`
  - 原因：`fail-closed` 保持；`samefile`/受限 fallback 符合计划；未放宽 Delta-2.5e / Delta-2.5b 契约

## Next Step
1. 提交 delta-2.5f
2. 推送远端
3. 观察新的 macOS `Validate macOS iOS Toolchain Report`
