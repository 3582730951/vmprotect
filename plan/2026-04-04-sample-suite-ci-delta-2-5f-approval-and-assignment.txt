# 2026-04-04 sample-suite CI Delta-2.5f 审批与分配

## Plan Verdict
- `Agent 1 (Planner / PM)`: `PASS`
- `Agent 2 (Engineer plan reviewer)`: `PASS`
- `Agent 3 (Security Reviewer)`: `PASS`

## Approval Basis
- 当前唯一 blocker 已收敛为 macOS `Validate macOS iOS Toolchain Report` 中 `plugin_build_command` 的 path-identity 误判。
- 计划边界已冻结为单文件、单段实现：
  - `/workspace/.github/workflows/sample-suite-eval.yml`
  - 仅允许修改 macOS `Validate macOS iOS Toolchain Report` 的内嵌 Python 段
- 任务卡已写死：
  - 解析 `-D<KEY>=...` / `-D<KEY>:<TYPE>=...`
  - 仅允许 `OSError` / `FileNotFoundError` 触发 fallback
  - `CMAKE_CXX_COMPILER` / `LLVM_DIR` 改为路径同一性校验
  - 保留 Delta-2.5e 的 `plugin_build_log` / `plugin_link_command` strict gate
  - 禁止触碰 Linux / Windows / Android validator 与 `build_macos_ios.sh`

## Implementation Assignment
- `Agent 2 (Engineer)` 负责唯一实现任务。
- 任务卡：
  - `/workspace/plan/2026-04-04-sample-suite-ci-delta-2-5f-phase-1-task-card.txt`

## PM Constraints
- PM 不写生产实现代码。
- PM 只负责：
  - reviewer 回收
  - 工程任务下发
  - 静态验证
  - 实现复审
  - 提交推送
  - CI 观察

## Acceptance Before Merge
- `Agent 2` 的实现 diff 只允许：
  - `/workspace/.github/workflows/sample-suite-eval.yml`
- PM 必须完成：
  - `git diff --name-only`
  - `git diff -U0 -- /workspace/.github/workflows/sample-suite-eval.yml`
  - `git diff --check`
  - `rg -n 'extract_cmake_define_paths|assert_cmake_define_matches_expected|plugin_build_command missing host compiler path|sidecar missing required token: plugin_build_command|plugin_link_command leaked dead_strip|plugin_link_command leaked ninja progress prefix' /workspace/.github/workflows/sample-suite-eval.yml`
  - `rg -n 'str\\(Path\\(kv\\[\"host_compiler_path\"\\]\\)\\.resolve\\(\\)\\) not in plugin_build_text|str\\(\\(llvm_prefix / \"lib\" / \"cmake\" / \"llvm\"\\)\\.resolve\\(\\)\\) not in plugin_build_text' /workspace/.github/workflows/sample-suite-eval.yml`
  - 任务卡中固定的 `python3 ... compile(..., "exec")` 语法门禁

## Next Step
1. 回收 plan reviewer
2. 下发单写者实现任务
3. PM 做静态验证
4. 三角色实现复审
5. 提交推送并观察 CI
