# 2026-04-04 sample-suite CI Delta-2.2 执行记录

## Scope
- 本轮执行的目标是关闭 run `23968822768` 中的 3 个 blocker：
  - Windows PowerShell Generic List 类型错误
  - Linux/Android `.ko` 的 `-r and -pie may not be used together`
  - macOS/iOS final driver 仍为 `clang++`

## Implemented Changes
- 提交：
  - `cbf277f4d94173488c8e48955f2037ca4600293a`
  - message: `fix: resolve sample-suite ci delta 2.2 blockers`
- 修改文件：
  - `/workspace/.github/workflows/sample-suite-eval.yml`
  - `/workspace/core/tools/sample_suite/build_linux_android.sh`
  - `/workspace/core/tools/sample_suite/build_macos_ios.sh`

## Local Verification
- `python3 -c 'import yaml; yaml.safe_load(open(".github/workflows/sample-suite-eval.yml"))'`
  - pass
- `bash -n /workspace/core/tools/sample_suite/build_linux_android.sh`
  - pass
- `bash -n /workspace/core/tools/sample_suite/build_macos_ios.sh`
  - pass
- 静态 gate 确认：
  - workflow 中已无 `System.Collections.Generic.List` / `.Add(`
  - `.ko` 两条链路已显式含 `-no-pie`
  - iOS report / validator 已拆出 `ios_link_driver_*`

## Remote Execution
- GitHub Actions run:
  - `23981450573`
- URL:
  - `https://github.com/3582730951/vmprotect/actions/runs/23981450573`
- 结果：
  - `failure`

## Actual Outcomes
- 已解决：
  - Windows 不再报 PowerShell `Argument types do not match`
  - Linux/Android `.ko` 不再报 `-r and -pie may not be used together`
- 未解决，且暴露出新的真实 blocker：
  1. Windows / `69946371263`
     - `Preflight LLVM Toolchain` 失败
     - 错误：`[FAIL] no usable LLVM root found`
     - 候选根缺 `LLVMConfig.cmake`
  2. macOS/iOS / `69946371267`
     - `Build iOS Mach-O Sample` 失败
     - 错误：`clang: error: unable to execute command: Segmentation fault: 11`
  3. Linux/Android / `69946371269`
     - `Build Linux Android Samples` 失败
     - 错误根因迁移为 Android NDK `clang-17` 在 `llvm::PassPlugin::Load(...)` 处崩溃
  4. `protect-and-evaluate`
     - 被上游 3 个 build job 失败联动 skip

## Root-Cause Snapshot
- Windows：
  - 当前问题已从 PowerShell 实现错误切换为 LLVM18 root / `LLVM_DIR` 契约不成立
- Android：
  - 当前问题已从 `.ko` PIE 冲突切换为 NDK clang17 加载 LLVM18 pass plugin 的 ABI 不兼容
- iOS：
  - 当前问题已从 final driver 选择错误切换为 Apple clang17 加载 LLVM18 pass plugin 的 ABI 不兼容

## Decision
- `Delta-2.2` 判定为：
  - `implemented but not accepted`
- 下一轮必须进入 `Delta-2.3`：
  - Windows：显式 provision pinned LLVM18
  - Android/iOS：拆成 `host LLVM18 protected compile` + `target-native object-only final link`
