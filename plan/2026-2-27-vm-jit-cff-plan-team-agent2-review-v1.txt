Agent2 (Engineer) 审核报告 - v1
日期：2026-02-27
结论：FAIL

不通过项
1. 缺少可直接实施的产物路径
- 计划未明确 `core/runtime/*`、`core/tests/*`、`core/passes/*` 的具体新增文件名，工程落地不可追踪。
2. 热点自动分析未定义归一化细节
- `*_norm` 没有给出计算方法（min-max/zscore）及冷启动默认值，结果不可复现。
3. 路由与预算耦合规则不完整
- `Cold + Non-Critical` 可选 `NONE/CFF_LITE`，但缺少明确切换阈值与 deterministic 决策规则。
4. 缺少 CI 失败码与阻断条件矩阵
- 仅写“hard-fail”，未给错误码/阶段，无法自动化运维。

整改建议
1. 增加“文件级实施清单 + 验收标准”。
2. 补全归一化协议与窗口冻结。
3. 补全 deterministic 路由条件。
4. 补全 CI 阶段与错误码映射。
