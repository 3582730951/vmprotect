# 2026-04-05 sample-suite CI Delta-2.5g 执行记录

## Scope
- delta: `2.5g`
- blocker: `macOS Validate macOS iOS Toolchain Report -> [FAIL] ios_user_helper_compile_command missing isysroot`
- implementation file:
  - `/workspace/.github/workflows/sample-suite-eval.yml`

## Plan Approval
- `Agent 1 (Planner / PM)`: `PASS`
- `Agent 2 (Engineer plan reviewer)`: `PASS`
- `Agent 3 (Security Reviewer)`: `PASS`

## Assignment
- `Agent 2 (Engineer)` 获得唯一实现文件所有权：
  - `/workspace/.github/workflows/sample-suite-eval.yml`
- 修改范围冻结为：
  - macOS `Validate macOS iOS Toolchain Report` 的内嵌 Python 段

## Implementation Summary
- 新增：
  - `extract_flag_value_paths(command_text: str, flag: str) -> list[Path]`
  - `assert_flag_path_matches_expected(command_text: str, command_key: str, flag: str, expected_path: Path, fail_msg: str) -> None`
- 固定：
  - `extract_flag_value_paths` 只提取 `-isysroot <path>` 与 `-isysroot=<path>`
  - `assert_flag_path_matches_expected` 只允许 `OSError` / `FileNotFoundError` 触发 fallback
- 替换：
  - 删除三条旧的 `"-isysroot" in text + str(...resolve()) in text` 逻辑
  - 改为三次 `assert_flag_path_matches_expected(..., "-isysroot", Path(kv["ios_isysroot"]), "[FAIL] ... missing isysroot")`
- 保持不变：
  - `if Path(kv["ios_isysroot"]).resolve() != ios_sdk: raise ...`
  - `assert_cmake_define_matches_expected(...)`
  - `plugin_build_log` / `plugin_link_command` strict gate
  - Linux / Windows / Android validator
  - `build_macos_ios.sh`

## PM Static Validation
- `git diff --name-only`
  - 结果：仅 `.github/workflows/sample-suite-eval.yml`
- `python3 ...` diff 边界脚本
  - 结果：`OK`
- `git diff --check`
  - 结果：通过
- `rg -n '^\\s*except Exception\\s*:' /workspace/.github/workflows/sample-suite-eval.yml`
  - 结果：0 命中
- `rg -n '^\\s*def extract_flag_value_paths\\(' /workspace/.github/workflows/sample-suite-eval.yml`
  - 结果：1 命中
- `rg -n '^\\s*def assert_flag_path_matches_expected\\(' /workspace/.github/workflows/sample-suite-eval.yml`
  - 结果：1 命中
- `rg -n 'assert_flag_path_matches_expected\\(ios_helper_text, \"ios_user_helper_compile_command\", \"-isysroot\", Path\\(kv\\[\"ios_isysroot\"\\]\\), \"\\[FAIL\\] ios_user_helper_compile_command missing isysroot\"\\)' /workspace/.github/workflows/sample-suite-eval.yml`
  - 结果：1 命中
- `rg -n 'assert_flag_path_matches_expected\\(protected_compile_text, \"ios_protected_compile_command\", \"-isysroot\", Path\\(kv\\[\"ios_isysroot\"\\]\\), \"\\[FAIL\\] ios_protected_compile_command missing isysroot\"\\)' /workspace/.github/workflows/sample-suite-eval.yml`
  - 结果：1 命中
- `rg -n 'assert_flag_path_matches_expected\\(command_text, \"ios_link_command\", \"-isysroot\", Path\\(kv\\[\"ios_isysroot\"\\]\\), \"\\[FAIL\\] ios_link_command missing isysroot\"\\)' /workspace/.github/workflows/sample-suite-eval.yml`
  - 结果：1 命中
- `rg -n 'if \"-isysroot\" not in ios_helper_text|if \"-isysroot\" not in protected_compile_text|if \"-isysroot\" not in command_text' /workspace/.github/workflows/sample-suite-eval.yml`
  - 结果：0 命中
- `rg -n '^\\s*def assert_cmake_define_matches_expected\\(' /workspace/.github/workflows/sample-suite-eval.yml`
  - 结果：1 命中
- `rg -n 'plugin_link_command leaked dead_strip' /workspace/.github/workflows/sample-suite-eval.yml`
  - 结果：1 命中
- `rg -n 'plugin_link_command leaked ninja progress prefix' /workspace/.github/workflows/sample-suite-eval.yml`
  - 结果：1 命中
- `python3 ... compile(..., "exec")`
  - 结果：`OK`

## Implementation Review
- `Agent 1 (Planner / PM)`: `PASS`
  - 原因：逻辑与任务卡一致；diff 边界正确；全部静态 gate 通过
- `Agent 2 (Engineer reviewer)`: `PASS`
  - 原因：三处 `-isysroot` 替换到位；新增 helper 满足机械规则；仍为最小语义改动
- `Agent 3 (Security Reviewer)`: `PASS`
  - 原因：`fail-closed` 保持；三处 path-identity 校验符合计划；未放宽 Delta-2.5f / Delta-2.5e / Delta-2.5b 契约

## Next Step
1. 提交 delta-2.5g
2. 推送远端
3. 观察新的 macOS `Validate macOS iOS Toolchain Report`
