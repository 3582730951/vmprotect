Agent2 (Engineer) 审核 v1
结论：FAIL

问题
1. 计划提到 `Anchor -> VM -> JIT -> CFF`，但未明确 VM/JIT 的注解映射冲突处理（同函数多注解时谁优先）。
2. 未定义 wrapper 的多插件参数格式（分隔符、重复参数、命令行优先级）。
3. 未给出 strip 触发条件（仅链接阶段？仅 ELF？共享库是否默认跳过？）。
4. 验收标准缺少“构建命令与判定脚本”。

建议
1. 增加注解优先级矩阵。
2. 明确 wrapper 参数协议。
3. 明确 strip 策略与 fail-closed 开关。
4. 增加自动化验证命令。
