# 2026-04-05 sample-suite CI Delta-2.5j 执行记录

## 状态
- `IMPLEMENTED`

## 触发 blocker
- `sample-suite-eval` run `23995324836`
- `build-windows` job `69982318594`
- 失败阶段：
  - `Build Windows Samples`
- 关键失败证据：
  - `eippf_string_protection_pass ignored -- Loadable modules not supported on this platform.`
  - `target_include_directories called with non-compilable target type`
  - `target_compile_definitions called with non-compilable target type`
  - `target_compile_features called with non-compilable target type`

## 已批准计划
- `/workspace/plan/2026-04-05-sample-suite-ci-delta-2-5j-plan.txt`
- `/workspace/plan/2026-04-05-sample-suite-ci-delta-2-5j-approval-and-assignment.txt`
- `/workspace/plan/2026-04-05-sample-suite-ci-delta-2-5j-phase-1-task-card.txt`

## 实现 owner
- `Agent 2 (Engineer)` 新实例
- 实际执行 agent：
  - `019d5c4c-607f-7c72-a27c-071f78ecb058`

## 实现结果
- 新增：
  - `/workspace/core/cmake/PassPluginCompat.cmake`
- 修改：
  - `/workspace/core/passes/CMakeLists.txt`
- 未修改：
  - `/workspace/core/CMakeLists.txt`
  - 任何 `.cpp/.hpp/.py/.ps1/.yml`
  - tests / workflow / sample-suite scripts

## PM 独立验收
### 静态 gate
- `git -C /workspace diff --name-only`
  - 结果：仅实现文件出现在 tracked diff 中
- `git -C /workspace diff --check`
  - 结果：通过
- helper / call-site / Apple / Windows / fail-closed 相关静态 gate
  - 结果：全部通过

### 本地 configure gate
- 命令：
  - `cmake -S /workspace/core -B /tmp/eippf_delta25j_probe -G Ninja -DLLVM_DIR=/usr/lib/llvm-14/lib/cmake/llvm -DEIPPF_BUILD_TESTS=OFF -DEIPPF_BUILD_POST_LINK_MUTATOR=OFF -DEIPPF_BUILD_DEX_TOOLCHAIN=OFF -DEIPPF_BUILD_SCRIPT_GUARD=OFF -DEIPPF_BUILD_IP_WEAVER=OFF -DEIPPF_BUILD_IP_WEAVER_IR=OFF -DEIPPF_BUILD_TOOLING=OFF`
- 结果：通过

### 额外 build gate
- 命令：
  - `cmake --build /tmp/eippf_delta25j_probe --target eippf_protection_suite_pass -j 4`
- 结果：通过
- 产物：
  - `passes/eippf_protection_suite_pass.so`

## 三角色实现复审
- `Architect Reviewer`: `PASS`
- `Engineer Reviewer`: `PASS`
- `Security Reviewer`: `PASS`

## 关键收口点
- `core/passes/CMakeLists.txt` 不再直接调用 `add_llvm_pass_plugin(...)`
- 所有 pass target 统一改走 `eippf_add_pass_plugin_target(...)`
- Windows 路径固定走 `SHARED` fallback
- Windows fallback 固定：
  - `TARGET LLVM` 优先
  - `llvm_map_components_to_libnames(...)` 次级
  - 两者都缺则 `FATAL_ERROR`
- target type 只接受：
  - `MODULE_LIBRARY`
  - `SHARED_LIBRARY`
- Windows 输出目录已同步到：
  - `RUNTIME_OUTPUT_DIRECTORY`
  - `LIBRARY_OUTPUT_DIRECTORY`
  - `RUNTIME_OUTPUT_DIRECTORY_RELEASE`
  - `LIBRARY_OUTPUT_DIRECTORY_RELEASE`

## 待执行
- 提交
- 推送
- 观察新的 `sample-suite-eval`
