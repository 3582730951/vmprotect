# 2026-04-05 sample-suite CI Delta-2.5g 审批与分配

## Plan Verdict
- `Agent 1 (Planner / PM)`: `PASS`
- `Agent 2 (Engineer plan reviewer)`: `PASS`
- `Agent 3 (Security Reviewer)`: `PASS`

## Approval Basis
- 当前唯一 blocker 已收敛为 macOS `Validate macOS iOS Toolchain Report` 中三处 `-isysroot` 的 path-identity 误判。
- 计划边界已冻结为单文件、单段实现：
  - `/workspace/.github/workflows/sample-suite-eval.yml`
  - 仅允许修改 macOS `Validate macOS iOS Toolchain Report` 的内嵌 Python 段
- 任务卡已写死：
  - `extract_flag_value_paths(...)`
  - `assert_flag_path_matches_expected(...)`
  - 三处 `-isysroot` 替换调用必须全部存在
  - `except Exception` 必须 0 命中
  - 边界 gate 必须按 hunk 完整区间校验
  - 保留 `assert_cmake_define_matches_expected` 与 `plugin_link_command` strict gate

## Implementation Assignment
- `Agent 2 (Engineer)` 负责唯一实现任务。
- 任务卡：
  - `/workspace/plan/2026-04-04-sample-suite-ci-delta-2-5g-phase-1-task-card.txt`

## PM Constraints
- PM 不写生产实现代码。
- PM 只负责：
  - reviewer 回收
  - 工程任务下发
  - 静态验证
  - 实现复审
  - 提交推送
  - CI 观察

## Acceptance Before Merge
- `Agent 2` 的实现 diff 只允许：
  - `/workspace/.github/workflows/sample-suite-eval.yml`
- PM 必须完成任务卡内全部静态 gate，且全部 PASS，才允许合入与推送。

## Next Step
1. 回收 plan reviewer
2. 下发单写者实现任务
3. PM 做静态验证
4. 三角色实现复审
5. 提交推送并观察 CI
