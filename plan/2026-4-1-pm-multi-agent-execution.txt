# 2026-04-01 PM 多 Agent 并行执行状态

## Phase 1：本轮已落地范围
- 已补齐 `contracts/runtime backend` 最小契约闭环：
  - `core/include/contracts/protection_contracts.hpp`
  - `core/runtime/include/runtime/backend_policy.hpp`
  - `core/runtime/include/runtime/backends/backend_registry.hpp`
  - `core/runtime/src/backend_policy.cpp`
  - `core/runtime/src/backends/backend_registry.cpp`
- 已补齐三条新产品线最小 CLI 骨架：
  - `core/post_link_mutator/`
  - `core/dex_toolchain/`
  - `core/script_guard/`
- 已补齐字符串锚点审计工具：
  - `core/tools/artifact_audit.py`
  - `core/tools/lexical_anchor_denylist.txt`
- 已补齐新增测试：
  - `contracts`
  - `runtime/backend`
  - `post_link_mutator`
  - `dex_toolchain`
  - `script_guard`
- 已修复 LLVM 14 兼容性断点：
  - `StringRef::starts_with` -> `startswith`
  - `JITEnclavePass` 的 `Triple` include fallback
  - `ip_weaver_ir` 的 `SROAPass` 兼容写法
- 已把环境能力不满足的测试改成显式 capability skip，而非无意义失败：
  - Vulkan instance 不可用
  - Rust/LLVM bitcode 版本不匹配

## Phase 2：验证结果
- `cmake -S /workspace/core -B /workspace/build_pm -G Ninja`
  - `PASS`
- `cmake --build /workspace/build_pm -j`
  - `PASS`
- `ctest --test-dir /workspace/build_pm --output-on-failure`
  - `PASS`
  - `20/20 tests passed`
- `cmake --build /workspace/build_pm --target perf_smoke`
  - `PASS`
  - OpenCL/Vulkan/Rust VM 覆盖中的环境不足已显式输出 `[SKIP]` 原因

## Phase 3：多 Agent 轨道追踪
- Agent 1（Planner / PM / main_agent）
  - 负责任务分解、文件所有权、验证命令、角色切换记录、最终收敛
- Agent 2（Engineer 轨）
  - Newton：只读审计 `contracts/runtime`，早期发现命名解析二义性
  - Darwin：只读审计 `dex_toolchain/script_guard`，确认主线骨架与 smoke test 已落地
  - Leibniz：只读审计 `post_link_mutator`，指出测试接线与产物契约缺口
- Agent 3（Security Reviewer 轨）
  - Averroes：独立安全审计，指出 placeholder mutator、bundle key 暴露、runtime JIT 约束未落地等高优先级问题

## Phase 4：决策记录
- Role Transfer
  - 由于并行工程 Agent 输出未直接合入主工作区，PM 临时接管共享集成切片，直接落地最小可编译闭环
  - 当前执行模式仍为 `real-multi-agent`，但 PM 对本轮改动不做自我批准，安全结论以独立 reviewer 输出为准
- Test Gate Decision
  - Vulkan 和 Rust VM 全覆盖测试的失败被判定为环境 capability gate，不是新代码逻辑回归
  - 处理方式不是静默移除测试，而是保留测试、显式输出 `[SKIP]` 原因

## Phase 5：已确认未解决问题
- `post_link_mutator` 仍是 placeholder：
  - 当前行为是复制产物并写出 `copied_without_mutation`
  - 这不满足 release 级保护要求，后续必须升级为真实 mutation + audit chain
- `dex_toolchain` / `script_guard` 仍存在 key 暴露问题：
  - manifest 中写入 `encryption_key`
  - bundle header 中写入 key marker
  - 这只适合作为结构骨架，不适合作为强保护版本
- iOS / kernel 约束仍未真正压到 runtime 执行路径：
  - policy 已声明
  - runtime 中的 JIT / executable page 路径尚未按 `ios_safe` / `kernel_safe` backend 彻底拆分
- `artifact_audit.py` 目前仍偏浅层：
  - 真实 section permission / import / signature 审计还未实现
  - 现在主要提供 lexical anchor 基线扫描与 self-test

## Phase 6：下一轮优先级
1. 把 `post_link_mutator` 从 placeholder 升级到真实变异链，并串上 artifact audit strict gate
2. 去掉 `dex/script` 的 key 明文暴露，改成真正的 key 派生/外部绑定方案
3. 拆分 `backend_ios_safe` 与 `backend_kernel_safe` 的 runtime 路径，彻底禁用 JIT/RWX 回流
4. 扩展 driver 产物精细分类：
   - `windows_driver_sys`
   - `linux_kernel_module_ko`
   - `android_kernel_module`

## Phase 7：第二轮已落地范围
- `post_link_mutator` 已从纯复制 placeholder 升级为真实 fail-closed 骨架：
  - 未识别产物直接失败
  - 识别 `desktop / ios / windows driver / linux ko / android ko`
  - 输出文件追加 mutation trailer，产物不再与输入字节完全相同
  - manifest 升级为 `ProtectionManifestV2` 形状
- `HardenTarget` 已补齐：
  - 自动解析 pass plugin 目标到真实路径
  - 自动回收已构建的 `eippf_post_link_mutator`
  - 自动接入 `artifact_audit.py --strict`
  - 新增 `NO_PASS_PLUGIN` 选项，允许只测 post-link/audit 链
- 新增严格验证：
  - `artifact_audit_strict_test`
  - `hardened_target_pipeline_test`
  - 该 pipeline test 已验证真实 `POST_BUILD` 链路会生成：
    - 变异后的产物
    - `.pre_eippf` 备份
    - manifest sidecar
    - audit sidecar

## Phase 8：第二轮验证结果
- `cmake -S /workspace/core -B /workspace/build_pm -G Ninja`
  - `PASS`
- `cmake --build /workspace/build_pm -j`
  - `PASS`
- `ctest --test-dir /workspace/build_pm --output-on-failure`
  - `PASS`
  - `22/22 tests passed`
- `cmake --build /workspace/build_pm --target perf_smoke`
  - `PASS`

## Phase 9：当前仍未解决的高优先级问题
- `artifact_audit.py --strict` 目前仍以 lexical anchor 为主，不是真正的完整 imports/sections/signature gate
- `ios_safe` / `kernel_safe` runtime 约束还未彻底从执行路径切开

## Phase 10：第三轮已落地范围
- `dex_toolchain` / `script_guard` 不再把 `encryption_key` 写入 manifest
- 两者也不再把 `input_hash_fnv1a64` 暴露到 manifest
- bundle header 中的 key marker 已移除，改为 `external_binding_required`
- smoke tests 已补充断言，防止 key 或 plaintext fingerprint 回归

## Phase 11：当前最高优先级剩余项
1. 将 `artifact_audit.py --strict` 从 lexical-only 提升到真实 imports/sections/signature gate
2. 拆分 `backend_ios_safe` / `backend_kernel_safe` 的 runtime 执行路径，彻底阻断 JIT/RWX 回流
3. 为 `dex_toolchain` / `script_guard` 引入真正的外部密钥绑定与后续 loader / launcher 消费链

## Phase 12：第三轮本次推进已落地范围
- runtime 安全后端约束已从 policy 声明推进到真实执行路径：
  - `core/runtime/include/runtime/memory_hal.hpp`
    - 新增编译目标感知 `configured_target_kind()`
    - 新增 `runtime_dynamic_code_allowed()`
    - `ios_appstore/windows_driver/linux/android .ko` 等 safe backend 禁止 `protect_rx`
    - Apple ARM 分配不再直接 `RWX`
  - `core/runtime/src/secure_ir_engine.cpp`
    - safe backend 下 `compile()` 直接 fail-closed
    - `execute()` 不再尝试进入 JIT 执行，转入 mitigation
- 构建链已开始向目标注入 runtime target 宏：
  - `core/cmake/HardenTarget.cmake`
    - 新增 `_eippf_classify_runtime_target_kind`
    - `eippf_harden_target()` 统一注入 `EIPPF_RUNTIME_TARGET_KIND`
- 新增 safe-backend 回归测试：
  - `core/tests/runtime/hal/memory_hal_safe_backend_test.cpp`
  - `core/tests/runtime/ir/secure_ir_engine_safe_backend_test.cpp`
  - `core/tests/CMakeLists.txt`
    - 新增 `ios_appstore` / `windows_driver` 两组测试目标
- `artifact_audit.py --strict` 已从 lexical gate 升级为多维 gate：
  - 继续保留 denylist / W+X / bundle invariant
  - 新增 imports 审计：
    - PE import table
    - ELF `DT_NEEDED`
    - Mach-O `LC_LOAD_*DYLIB`
  - 新增 symbols 审计：
    - PE export table
    - ELF `SHT_SYMTAB/SHT_DYNSYM`
    - Mach-O `LC_SYMTAB`
  - 新增 signature 审计：
    - `.sys` -> PE security directory
    - `.ko` -> module signature trailer
    - Mach-O -> `LC_CODE_SIGNATURE`
  - strict mode 新增：
    - `imports_policy_failed`
    - `symbols_policy_failed`
    - `signature_required_but_missing`
    - 对 parse failure 的 fail-closed
- `artifact_audit_strict_test` 已扩展真实严格场景：
  - clean PE pass
  - denylisted string fail
  - W+X section fail
  - suspicious import (`dbghelp.dll`) fail
  - unsigned `.sys` fail
  - signed `.sys` pass

## Phase 13：本轮多 Agent 轨道追踪
- Agent 1（Planner / PM / main_agent）
  - 负责切分本轮为 `runtime-safe` 与 `artifact-audit` 两条并行线
  - 负责集成、复构建、更新执行记录
- Agent 2（Engineer / Boyle）
  - 已落地并回传 `memory_hal.hpp` / `secure_ir_engine.cpp` 的 runtime-safe 设计
  - PM 已按该设计完成主工作区集成与测试补齐
- Agent 3（Engineer / Mill）
  - 在 `artifact_audit` 轨未形成代码产出
  - 视为本轮执行失败，不计入完成证据
- Reviewer 轨（本轮盲审已启动）
  - `Mencius`：独立 Security Reviewer
  - `Boole`：Independent Validation Reviewer

## Phase 14：Role Transfer 与质量决策
- Role Transfer
  - `artifact_audit` 轨因 worker 未产出，PM 接管共享集成切片，直接完成脚本与测试修改
  - `runtime-safe` 轨采用 Engineer 设计 + PM 集成方式推进
- Strike Ledger（本轮）
  - `Mill / artifact-audit-track`
    - reason: `missing_evidence`
    - strike_count: `1`
    - reassignment: `false`

## Phase 15：第三轮验证结果
- `python3 -m py_compile /workspace/core/tools/artifact_audit.py`
  - `PASS`
- `python3 /workspace/core/tools/artifact_audit.py --self-test`
  - `PASS`
- `cmake -S /workspace/core -B /workspace/build_pm -G Ninja`
  - `PASS`
- `cmake --build /workspace/build_pm -j`
  - `PASS`
- `ctest --test-dir /workspace/build_pm --output-on-failure`
  - `PASS`
  - `26/26 tests passed`
- `cmake --build /workspace/build_pm --target perf_smoke`
  - `PASS`

## Phase 16：当前剩余高优先级问题
1. `artifact_audit` 现在已具备 imports/symbols/signature gate，但对真实签名有效性的校验仍是 presence-based，不是完整 trust chain 验证
2. `HardenTarget` 的 runtime target 分类目前仍基于目标名启发式匹配，后续需要演进为显式 manifest / CMake 参数驱动
3. `dex_toolchain` / `script_guard` 仍缺真正的外部密钥绑定消费链

## Phase 17：针对 reviewer blocking finding 的修正
- 为解决 “compile-unit mismatch / hardened path false-pass”：
  - `core/runtime/include/runtime/memory_hal.hpp`
    - 新增 linked target anchor 读取逻辑
    - 优先读取链接期注入的 `eippf_runtime_target_kind_anchor`
  - `core/cmake/HardenTarget.cmake`
    - `eippf_harden_target()` 现在会为每个目标生成并注入
      - `${target}.eippf_runtime_target_anchor.cpp`
      - `extern "C" const unsigned int eippf_runtime_target_kind_anchor = ...`
- 新增 hardened target 级验证，不再只依赖手工测试宏：
  - `core/tests/tools/hardened_target_runtime_kind_test.cpp`
    - 验证 hardened 目标自身可读到正确 `target_kind`
  - `core/tests/tools/hardened_target_secure_ir_engine_test.cpp`
    - 直接验证 `eippf_runtime` 内 `SecureIREngine` 在 hardened target 下也会 fail-closed
  - `core/tests/tools/CMakeLists.txt`
    - 新增：
      - `hardened_target_ios_runtime_kind_test`
      - `hardened_target_windows_driver_runtime_kind_test`
      - `hardened_target_ios_secure_ir_engine_test`
      - `hardened_target_windows_driver_secure_ir_engine_test`
    - 全部通过 `eippf_harden_target(NO_MUTATION NO_PASS_PLUGIN)` 接入真实链路
- `core/tests/CMakeLists.txt`
  - `perf_smoke` 已补齐新增 safe-backend / hardened-target 用例

## Phase 18：Reviewer 结论与处置
- Security Reviewer（Mencius）
  - 初审：
    - `critical`：hardened path 未真正证明到 runtime 库执行路径
    - `high`：签名 gate 仅做存在性检查
    - `high`：驱动签名 requirement 依赖扩展名
    - `medium`：target-kind 仍基于目标名启发式
  - 处置：
    - `critical` 已修复并补 hardened target 级测试
    - 其余列入残余风险，不在本轮扩展为完整签名真实性验证
  - 复审：
    - `PASS with residual risks`
- Independent Validation Reviewer（Boole）
  - 初审：
    - `high`：hardened path 未验证，存在 false-pass
    - `high`：签名 gate 仅为 presence-based
    - `medium`：ELF/Mach-O strict 回归测试不足
  - 处置：
    - hardened path 问题已通过 target anchor + hardened target tests 修复
    - 签名真实性和 ELF/Mach-O 覆盖保留为下一轮 gap
  - 最终状态：
    - 本轮 blocking 点已解除

## Phase 19：最新验证结果
- `cmake -S /workspace/core -B /workspace/build_pm -G Ninja`
  - `PASS`
- `cmake --build /workspace/build_pm -j`
  - `PASS`
- `ctest --test-dir /workspace/build_pm --output-on-failure`
  - `PASS`
  - `30/30 tests passed`
- `cmake --build /workspace/build_pm --target perf_smoke`
  - `PASS`
  - 新增用例已纳入：
    - `secure_ir_engine_ios_appstore_safe_test`
    - `secure_ir_engine_windows_driver_safe_test`
    - `memory_hal_ios_appstore_safe_test`
    - `memory_hal_windows_driver_safe_test`
    - `hardened_target_ios_runtime_kind_test`
    - `hardened_target_windows_driver_runtime_kind_test`
    - `hardened_target_ios_secure_ir_engine_test`
    - `hardened_target_windows_driver_secure_ir_engine_test`

## Phase 20：当前残余风险
1. `signature_state_passed` 目前仍是 presence-based，不是 cryptographic authenticity / trust-chain / revoke-state 验证
2. `linked target anchor` 当前在 GNU/Clang 路径已被证明有效；`MSVC` 侧仍需等价机制收敛
3. `HardenTarget` 的 target 分类仍基于目标名启发式，下一轮应升级为显式 target kind 参数或 manifest 绑定
4. `artifact_audit_strict_test` 仍以 PE/.sys 为主，ELF `.ko` / Mach-O 的 strict 回归矩阵还不完整
