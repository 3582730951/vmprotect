# 2026-04-04 sample-suite CI Delta-2.5b 审批与分配

## 审批结论
- `Agent 1 (Planner / PM)`：`PASS`
  - 原因：
    - 范围严格锁定在 `core/tools/sample_suite/build_macos_ios.sh`
    - superseding 边界只替代 Delta-2.5a 的单一提取细节
    - Phase、任务卡、验收门、回滚条件完整
    - 先 exact-one 再归一化 的顺序已冻结
- `Agent 2 (Engineer Reviewer)`：`PASS`
  - 原文摘录：
    - 计划和任务卡都把改动严格限定在单文件 `core/tools/sample_suite/build_macos_ios.sh`
    - 实现路径是唯一的，已固定为“保留候选提取 + exact-one 判定 + 前缀归一化 + 校验写 sidecar”
    - 明确要求先做 `exact-one`，再做 `[n/m]` 前缀归一化，顺序无歧义
    - 禁止项完整覆盖了扩范围风险
    - 任务步骤已机械化到命令级
- `Agent 3 (Security Reviewer)`：`PASS`
  - 原文摘录：
    - exact-one gate 被明确保留，原始候选不是 1 条时仍直接失败
    - `-Wl,-dead_strip` 检查被明确保留，且继续对 sidecar 生效
    - `PLUGIN_LINK_COMMAND_PATH` 明确要求写入去掉 `[n/m]` 前缀后的规范化命令
    - `0` 条和 `>1` 条候选都没有被降级成 warning，仍是 fail-closed
    - 作用域被锁死在 `build_macos_ios.sh` 单文件

## 批准结果
- `Delta-2.5b` 计划已批准，可以进入实现阶段。

## 分配
- `Agent 2 (Engineer)` 实施：
  - 文件：`/workspace/core/tools/sample_suite/build_macos_ios.sh`
  - 任务卡：`/workspace/plan/2026-04-04-sample-suite-ci-delta-2-5b-phase-1-task-card.txt`
  - 禁止修改任何其它生产文件
- `Agent 1 (Planner / PM)`：
  - 实施后只做静态核验、复审组织、提交推送、CI 观察
- `Agent 3 (Security Reviewer)`：
  - 实施完成后只审 fail-closed、sidecar 真值、scope 漂移

## 实施前核验
- 当前 blocker 固定为：
  - macOS `build-macos-ios` 中 plugin link-line 提取误判 `0` 条
- 当前不纳入 blocker：
  - `build-windows` 状态
  - Linux / Android lane
